name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "--cfg reqwest_unstable"

jobs:
  check:
    name: Check, Lint, Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Format check
        run: |
          cd webclaw-http && cargo fmt --check
          cd ../bench && cargo fmt --check

      - name: Clippy
        run: cd webclaw-http && cargo clippy -- -D warnings

      - name: Unit tests
        run: cd webclaw-http && cargo test --lib

      - name: Hermetic tests
        run: cd webclaw-http && cargo test --test unit

  integration:
    name: Integration Tests (network)
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Fingerprint tests
        run: cd webclaw-http && cargo test --test fingerprint

      - name: HTTP method tests
        run: cd webclaw-http && cargo test --test http_methods

  sync-downstream:
    name: Sync to webclaw core
    runs-on: ubuntu-latest
    needs: [check]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Trigger core dependency update
        run: |
          gh api repos/0xMassi/webclaw/dispatches \
            -f event_type=tls-updated \
            -f client_payload[sha]=${{ github.sha }}
        env:
          GH_TOKEN: ${{ secrets.SYNC_PAT }}
